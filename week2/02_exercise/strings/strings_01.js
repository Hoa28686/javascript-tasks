/* Declare three string variables using single quotes, double quotes, and backticks. For example:
'Single quoted string'
"Double quoted string"
`Template literal string`
Print all three strings using console.log(). */

let single = 'Single quoted string';
let double = "Double quoted string";
let backtick = `Template literal string`;

console.log(single);
console.log(double);
console.log(backtick);